export interface IPages {
  label: string;
  path: string;
  fragment?: string;
}

export interface IContactData {
  mail: string;
  phone: string;
  address: string;
  city: string;
  postCode: string;
}
